; Ensure Asterisk actually listens on UDP 5060
[transport-udp-local]
type=transport
protocol=udp
bind=0.0.0.0:5060
; If you need NAT, set these to your public IP (optional)
;external_signaling_address=YOUR_PUBLIC_IP
;external_media_address=YOUR_PUBLIC_IP
allow_reload=yes